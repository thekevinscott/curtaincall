name: Patch Release (Nightly)

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/publish.yml
    with:
      bump_type: patch
    permissions:
      contents: write
      id-token: write
